package noteApp.utils.FileArrange;

/**
 * Sorting type enum for deciding how to sort Notes
 */
public enum SortingType {
    ASCENDINGTAG, DESCENDINGTAG,
    ASCENDINGDATE, DESCENDINGDATE,
    DESCENDINGNAME, NONE
}
